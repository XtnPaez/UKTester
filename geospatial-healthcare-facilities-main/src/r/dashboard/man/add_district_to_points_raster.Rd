% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_district_to_points.R
\name{add_district_to_points_raster}
\alias{add_district_to_points_raster}
\title{Assign district to points using fine raster zonal majority (generalized)}
\usage{
add_district_to_points_raster(
  destinations,
  districts,
  pop_raster_path,
  district_col = "name",
  fine_res = 100,
  proj_crs = NULL
)
}
\arguments{
\item{destinations}{Tibble/data.frame with lon, lat columns}

\item{districts}{An sf object of districts (must have a unique name/id column)}

\item{pop_raster_path}{Path to population raster (for extent, resolution, and CRS)}

\item{district_col}{Name of the column in \code{districts} to use as the district label (default: "name")}

\item{fine_res}{Resolution for fine rasterization in meters (default: 100)}

\item{proj_crs}{Optional: EPSG code for projected CRS (default: auto-UTM based on centroid)}
}
\value{
A tibble with district column added
}
\description{
Rasterizes districts at fine resolution and assigns each point the district of the cell it falls in.
}
